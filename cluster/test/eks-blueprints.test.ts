import * as cdk from 'aws-cdk-lib';
import { Template } from 'aws-cdk-lib/assertions';
import * as EksBlueprints from '../lib/eks-sample-cluster';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/eks-blueprints-stack.ts
test('EKS Cluster Created', () => {
  // const app = new cdk.App();
  //   // WHEN
  // const stack = new EksBlueprints.EksSampleCluster(app, 'MyTestStack');
  //   // THEN
  // const template = Template.fromStack(stack);
  // template.hasResourceProperties('AWS::EKS::Cluster', {
  //     Name: "MyTestStack"
  // });
  // template.hasResourceProperties('AWS::EKS::Addon', {
  //     Name: "Karpenter"
  // });
  
});
